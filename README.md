# Difference_Drag

Orekit-based differential-drag simulator (LEO, 16U-class), benchmarked against MicroCosm.

## Structure
- `src/` : simulator source
- `configs/` : input JSON configs
- `outputs/` : generated CSV outputs (not committed)
- `data/` : orekit-data instructions (not committed)